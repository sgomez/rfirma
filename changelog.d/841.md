### Fixed
- Un `ver` dentro de una operación que llega por un canal ya abierto se
  ignora, como en AutoFirma: la versión la fija `v` al abrir el canal, y `ver`
  solo se comprueba en el camino del servidor intermedio (#841).

[package]
name = "token-flags-probe"
version = "0.1.0"
edition = "2021"
publish = false

# Sonda del sondeo #117: misma versión de `cryptoki` que rfirma-app.
[dependencies]
cryptoki = "0.12"

[workspace]

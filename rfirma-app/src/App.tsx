/**
 * La ventana vacía del andamiaje (#47).
 *
 * NO añadas aquí pantallas, componentes ni cadenas traducidas: el sistema de
 * diseño (`docs/design/design-system.md`) y la internacionalización pertenecen
 * a otros sub-issues de #46. Este componente existe para que `just dev` abra
 * una ventana y para que `vitest` tenga algo que renderizar.
 */
export function App() {
  return (
    <main aria-label="rfirma">
      <h1>rfirma</h1>
    </main>
  );
}

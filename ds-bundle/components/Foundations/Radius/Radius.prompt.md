# Radius

`--rf-radius-sm` 4px · `md` 6px · `lg` 8px · `xl` 16px · `pill` 9999px.

`md` (6px) es el valor por defecto: botones, tarjetas y campos. `xl` se reserva
a diálogos. `pill` solo para insignias y botones de acción secundaria en barras.
